import { AppPage } from './app.po'

describe('projet-industriel App', () => {
  let page: AppPage

  beforeEach(() => {
    page = new AppPage()
  })

  it('cheese the test', () => {
    expect(true).toEqual(true)
  })
})
